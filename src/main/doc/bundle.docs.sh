#!/bin/bash

## Bundle: bash-it
## Description: Minimal TAP test runner and `expect` assertion framework

BUNDLE_DOCS_DIR=$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)

source "${BUNDLE_DOCS_DIR}/generator.sh"
source "${BUNDLE_DOCS_DIR}/cli.sh"


